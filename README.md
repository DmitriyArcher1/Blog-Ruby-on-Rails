<div align="center">
    <h1>:star: Фото-блог на Ruby on Rails :star:</h1>
</div>


<div align="center">
    <h1> 🚀 Инструкция по инициализации и быстрому запуску приложения</h1>
</div>

Это руководство поможет вам быстро инициализировать и запустить приложение Ruby on Rails (представленное образом blog-ruby-on-rails:latest) с базой данных MySQL (образ mysql:8.0) используя Docker Compose.
___

## Содержание
1. [Предварительные требования](#-предварительные-требования)
2. [Структура проекта](#-структура-проекта)
3. [Создание файла docker-compose.yml](#-создание-файла-docker-composeyml)
4. [Инициализация базы данных и запуск приложения](#-инициализация-базы-данных-и-запуск-приложения)
5. [Доступ к приложению](#-доступ-к-приложению)
6. [Полезные команды Docker Compose](#-полезные-команды-docker-compose)
7. [Устранение неполадок](#-устранение-неполадок)
___
## 🛠️ Предварительные требования
Перед начало необходимо убедиться, что у вас установлен **Docker** и **Docker Compose.**

- Docker Engine. [Инструкции по установке Docker](https://docs.docker.com/engine/install/)
- Docker Compose. Обычно он устанавливается вместе с Docker Desktop. Если его нет по какой-либо причине, то [следуйте данным требованиям](https://docs.docker.com/compose/install/)
- MySQL. Чтобы приложение корректно работало вам также необходимо установить mysql. [Установка MySQL](https://dev.mysql.com/downloads/mysql/). Установка под разные ОС может отличаться, поэтому стоит детальнее вникнуть в то, как установить базу данных. При желании вы можете также установить графическую версию в дополнение, чтобы было удобнее смотреть за данными.

Чтобы проверить установку Docker и Docker Compose выполните следующие команды в терминале:

```bash
docker --version
docker-compose --version
```
___
## 📂 Структура проекта
Для данного руководства потребуется лишь один файл - **docker-compose.yml.**
Он нужен для того, чтобы образы (Image) собрались в контейнеры для работы приложения и базы данных:
```
── docker-compose.yml
```
___
## 📝 Создание файла docker-compose.yml
Создайте пустую директорию и в ней же создайте файл **docker-compose.yml.** Никакие другие файлы, кроме этого вам не потребуются.
```bash
mkdir photo-blog-app
cd photo-blog-app
touch docker-compose.yml
```
Далее скопируйте в данный файл следующее содержимое:
```docker
version: "3.9"

services:
  db:
    image: archero01/mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: qwerty2007
      MYSQL_DATABASE: my_blog_development
    volumes:
      - db_data:/opt/homebrew/var/mysql/
    ports:
      - "3306:3306"

  web:
    image: archero01/blog-ruby-on-rails-web:latest
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 3500 -b '0.0.0.0'"
    volumes:
      - .:/app
    ports:
      - "3500:3500"
    depends_on:
      - db
    environment:
      RAILS_ENV: development
      DATABASE_URL: mysql2://root:qwerty2007@db:3306/my_blog_development

volumes:
  db_data:
```
___
## 🚀 Инициализация базы данных и запуск приложения
Теперь, когда у вас есть файл docker-compose.yml вы можете запустить приложение.

1. Запуск образов и сборка их в контейнеры
```bash
docker-compose up
```
2. Инициализация базы данных.

Для работы приложения необходимо применить миграции к БД. Выполните следующую команду:
```bash
docker compose exec web bundle exec rails db:migrate
```
___
## 🌐 Доступ к приложению
Теперь, когда миграции успешно применены и контейнеры функционируют, зайдите в браузер по адресу: 

URL: http://localhost:3500/

Вы должны увидеть страницу фото-блога, где нет постов.
___
## 💡 Полезные команды Docker Compose
Показать статус запущенных контейнеров и Image:
```bash
docker-compose ps
```

Посмотреть логи в реальном времени:
```bash
dockercompose logs -f
```

Посмотреть логи конкретного контейнера. В нашем случае web:
```bash
docker-compose logf -f web
```

Выполнить команду внутри контейнера (открыть консоль rails):
```bash
docker-compose exec web bundle exec rails console
```

Перезапустить контейнер:
```bash
docker-compose restart web
```

(Если есть Dockerfile) Собрать и запустить контейнеры:
```bash
docker-compose up --build
```
___
## ⚠️ Устранение неполадок

- **"Cannot connect to the Docker daemon"** - убедитесь, что Docker запущен на вашем компьютере.
- **"Ошибка подлключения к базе данных Rails"**:
    1. Проверьте логи контейнера web:
    ```bash
    docker-compose logs web
    ```
    2. Убедитесь, что переменные окружения в docker-compose.yml совпадают с и db и web;
    3. Есть вероятность, что база данных ещё не полностью инициализировалась. Подождите ещё немного и затем выполните миграции ещё раз;
- **Контейнер db или web постоянно перезапускается или выходит**:
    1. Посмотрите логи данного контейнера:
    ```bash
    docker-compose logs <имя-контейнера>
    ```
    2. Возможно есть конфликт портов. Вам нужно убедиться, что порты 3500 и 3306 не заняты другими приложениями или сервисами на хосте;
- **Rails приложение не запускается после применения миграций**:
    1. Убедитесь, что команда запуска сервера Rails корректна